추천 서비스 요소 추가 방법

1. DataFetchService에 본인 다룰 데이터를 가져오는 메서드 추가
2. DataFetchServiceImpl에서 해당 메서드를 구현합니다.
3. RecommendMapper에 새로운 SQL 쿼리를 추가합니다.

현재는 calculateScores에서 치안에 관련된걸 직접 계산한다음 그대로 점수를 넘기는 형태이지만
치안에 관련된건 치안점수 메서드로 분리하고 calculateScores에서는 치안요인점수, 다른요인점수들을 받아서 평균적인 점수를 계산한 뒤 정렬해서 결과로 반영하는 방식으로
리팩토링 예상중

4. map.jsp에서 중요도 선택을 넘겨받아 배율을 적용한 본인의 계산 로직 추가(RecommendServiceImpl)

아마 이 부분만 수정하면 결과적으로 정렬, 탑5반환부분은 수정할일이 없을테니 정상적으로 동작할것으로 예상됩니다.

다만 주의할점은 데이터의 양이 많을 경우 실행 시간이 길어져서 발생하는 문제에 대해서는 아직 예측이 안되고 있습니다.